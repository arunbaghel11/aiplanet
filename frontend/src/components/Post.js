// Post.js placeholder
import React from 'react';

const Post = ({ content }) => {
    return (
        <div className="post">
            <p>{content}</p>
        </div>
    );
};

export default Post;
